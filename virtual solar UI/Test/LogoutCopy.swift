//
//  LogoutCopy.swift
//  virtual solar UI
//
//  Created by Lachlan Jiang on 27/4/2025.
//

/*
func logout() {
    do {
        try Auth.auth().signOut()
        isLoggedOut = true
    } catch let error {
        print("❌ Error signing out: \(error.localizedDescription)")
    }
}

Button(action: logout) {
    Text("Logout")
        .font(.subheadline)
        .foregroundColor(.white)
        .padding(8)
        .background(Color.red)
        .cornerRadius(8)
}
*/
